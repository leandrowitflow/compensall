/** Google Tag Manager container ID. */
export const GTM_ID = "GTM-T5WPLQBL";

/** Legacy GA4 ID — load GA via GTM tags, not a second hard-coded gtag. */
export const GA_MEASUREMENT_ID = "G-ZLJS0RZ0XS";

/** Meta (Facebook) Pixel ID — loaded in-app behind analytics consent (not via GTM). */
export const META_PIXEL_ID = "917764128053137";

/** Fired when the cookie banner saves a choice so analytics can start/stop. */
export const COOKIE_CONSENT_CHANGED_EVENT = "compensall:cookie-consent-changed";
